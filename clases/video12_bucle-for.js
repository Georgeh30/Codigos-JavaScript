/* Uso del Bucle For en su forma original */

/* Bucle For */
for (let index = 3; index <= 10; ++index) {
    console.log(index);
}

let number = [3,4,5,6,7,8];

for (let index = 0; index < number.length; ++index) {
    console.log(`index vale ${index} y el valor de esa
    posición en el array es ${number[index]}`);
}

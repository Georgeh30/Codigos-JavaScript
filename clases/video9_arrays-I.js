// USO BASICO DE ARRAY

let numeros = [1, 2, 3, 4, 5];
let palabras = ['hola', 'estamos', 'en', 'youtube']

// Suma de dos posiciones array
console.log(numeros[0] + numeros[1]);

// conteo de numero de letras de una posicion array
console.log(`La palabra (${palabras[1]}) tiene ${palabras[1].length} letras`);

// Imprimimos todo los datos dentro del array
console.log(numeros);
